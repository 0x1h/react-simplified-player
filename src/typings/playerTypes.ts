export interface SongContentType {
    song_name?: string,
    artist?: string,
    song_cover?: string,
    isLoading: boolean
}

export interface QueueType {
        song_cover?: string,
        song_title?: string,
        song_artist?: string,
        url: string
    
}